workflows:
  syncSpecToCollection:
    - spec: ../swaggerhub_apis/life360/preflight-checks-1.0.0.yaml
      collection: ../postman/collections/[Baseline] preflight-checks
    - spec: ../swaggerhub_apis/life360/preflight-checks-1.0.0.yaml
      collection: ../postman/collections/[Smoke] preflight-checks
    - spec: ../swaggerhub_apis/life360/preflight-checks-1.0.0.yaml
      collection: ../postman/collections/[Contract] preflight-checks
